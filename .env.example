# ===================================================================
# Environment Configuration Template
# ===================================================================
# SECURITY WARNING: Copy this to .env.dev or .env.prod and fill in
# your actual values. NEVER commit .env.dev or .env.prod to Git!
# ===================================================================

# Django Settings
# Generate a secure key with: python -c "import secrets; print(secrets.token_urlsafe(50))"
SECRET_KEY=REPLACE-WITH-YOUR-SECURE-SECRET-KEY

# Debug mode (ALWAYS False in production)
DEBUG=False

# Allowed hosts (comma-separated, no spaces)
# Development: localhost,127.0.0.1
# Production: yourdomain.com,www.yourdomain.com
ALLOWED_HOSTS=localhost,127.0.0.1

# HTTPS Settings (set to True in production with HTTPS)
USE_HTTPS=False

# Additional CORS origins (comma-separated)
# Example: https://yourdomain.com,https://www.yourdomain.com
CORS_ALLOWED_ORIGINS_EXTRA=

# Additional CSRF trusted origins (comma-separated)
# Example: https://yourdomain.com,https://www.yourdomain.com
CSRF_TRUSTED_ORIGINS_EXTRA=

# Database Configuration (PostgreSQL for production)
# Leave empty to use SQLite (development only)
# Format: postgresql://username:password@host:port/database
DATABASE_URL=

# Email Configuration (optional)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
