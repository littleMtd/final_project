# ===================================================================
# Development Environment Configuration (Ready to Use)
# ===================================================================
# Quick Start: Copy this file to .env.dev and run Docker
#   Windows: copy .env.dev.example .env.dev
#   macOS/Linux: cp .env.dev.example .env.dev
# ===================================================================

# Django Settings
SECRET_KEY=dev-insecure-CHANGE-THIS-IN-PRODUCTION-MqXT21vNdudJ0JpQhu0pprY6sNj51X7iDDTyFi4la0ziSJ_B

# Debug mode (ALWAYS False in production)
DEBUG=True

# Allowed hosts (comma-separated, no spaces)
ALLOWED_HOSTS=localhost,127.0.0.1

# HTTPS Settings (set to True in production with HTTPS)
USE_HTTPS=False

# Additional CORS origins (comma-separated)
CORS_ALLOWED_ORIGINS_EXTRA=

# Additional CSRF trusted origins (comma-separated)
CSRF_TRUSTED_ORIGINS_EXTRA=

# Database Configuration (Leave empty for SQLite)
DATABASE_URL=

# Email Configuration (optional)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
